{
  "name": "app-studio-starter",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd orchid dev",
    "generate-routes": "bun run --cwd orchid generate-routes",
    "build": "bun run --cwd orchid registry:build",
    "build:app": "bun run --cwd app build"
  }
}
